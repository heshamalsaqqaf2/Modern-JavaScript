/** 
 * @summary
 *     1- BigInt handles numbers larger than Number.MAX_SAFE_INTEGER
 *     2- Use the n suffix or BigInt() to create BigInts
 *     3- Can't mix BigInts with regular numbers in operations
 *     4- Useful for:
 *          - Large IDs
 *          - Financial calculations
 *          - High-precision measurements
 *          - Default values
 *     5- Has some limitations (no decimals, no Math object methods)
 *     6- Should be used only when regular numbers aren't sufficient
*/