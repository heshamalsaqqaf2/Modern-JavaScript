'use strict';

// Todo:The while loop is useful when you don't know in advance how many times the loop should run.
let count = 0;
while (count < 5) {
    console.log(`Count is: ${count}`);
    count++;
}